# Data Files

Place the exported JSON files (maps, items, spells, commerce, monsters, mappings, graphics) inside this directory when running the MonoGame client outside of the repository root. During development the client automatically falls back to `Docs/Formats/Samples`. EOF
